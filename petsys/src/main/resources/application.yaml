spring:
  application:
    name: petsys

  datasource:
    url: jdbc:h2:mem:petsysdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081